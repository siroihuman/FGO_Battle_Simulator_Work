# FGO Battle Simulator Work 共通作業規則

このファイルは、人間・ChatGPT・Codexを含むすべての作業者に適用します。

## リポジトリを唯一の正本とする

チャット履歴、要約、記憶だけを根拠に実装してはいけません。チャットを変更しても認識が揺れないよう、リポジトリ内の確定仕様、決定記録、現在状況、テストを唯一の正本として扱います。

現在のユーザーから明示された新しい指示が既存文書と衝突した場合は、新しい指示を優先します。ただし、実装と同じ変更内で仕様書、決定記録、必要なテストも更新してください。

## 作業開始前に必ず読むもの

1. `AGENTS.md`
2. `project-manifest.json`
3. `docs/INDEX.md`
4. `docs/PROJECT_RULES.md`
5. `docs/IMPLEMENTATION_STATUS.md`
6. 担当する役割の `docs/roles/*.md`
7. 関係する `docs/specs/*.md`
8. 関係する `docs/templates/*.md`
9. 実際の実装ファイルと既存テスト

読み終える前にコードやデータを変更してはいけません。

## 情報の優先順位

1. 現在のユーザーによる明示的な指示
2. `project-manifest.json` と `docs/PROJECT_RULES.md`
3. `docs/specs/` の確定仕様
4. `docs/DECISION_LOG.md`
5. `docs/IMPLEMENTATION_STATUS.md`
6. PR・Issueの内容
7. チャット履歴や記憶

下位の情報だけを根拠に、上位の情報と異なる挙動を追加してはいけません。

## 変更時の必須規則

- 参照したURLと確認した効果をデータまたはPRへ記録する。
- 強化前が不要と指定されているものは実装しない。
- 一般的な効果は共通処理へ登録する。
- 特殊な付与条件は再利用可能な共通トリガーへ登録する。
- 固有処理は対象ごとの個別ファイルへ分離する。
- 固有処理が不要な対象には個別ファイルを作らない。
- サーヴァントや敵の固有処理を巨大な戦闘処理へ直接追加しない。
- 同じ効果を別名の専用処理として重複実装しない。
- UI内でダメージ等を再計算せず、必ず戦闘エンジンの結果を表示する。
- 仕様が不明な場合は推測で確定せず、資料確認またはユーザー確認を行う。
- バグ修正には、同じ不具合を再発させないテストを追加する。
- データ追加にも、読み込み・必須値・代表効果のテストを追加する。
- 変更後に `npm test` を実行する。

## ID規則

- 参照サイトのページ番号を内部IDとして使用しない。
- 公式サーヴァントは可能な限りFGOのサーヴァントNo.を基準にする。
- オリジナル対象はユーザーが定めたNo.または変更しない安定IDを使用する。
- 同一サーヴァントを複数編成した場合、戦闘中は別々の個体IDを発行する。
- IDを変更する場合は保存データの移行処理を用意する。

## 仕様変更時に同時更新するもの

- `project-manifest.json` の重要値
- 該当する `docs/specs/*.md`
- `docs/PROJECT_RULES.md` の要約
- `docs/DECISION_LOG.md`
- `docs/IMPLEMENTATION_STATUS.md`
- 関係する自動テスト
- 必要に応じてテンプレート

仕様書だけ、またはコードだけを更新した状態で完了としてはいけません。

## 完了報告に含めるもの

- 変更内容
- 参照資料
- 通常処理／限定的例外処理／固有例外処理の分類
- 実行したテスト
- 未実装または未確認の項目
- 変更した仕様書
- 次の担当者が読むべきファイル

## 禁止事項

- チャットに書かれただけの未確定案を実装する。
- 特殊勝利条件・特殊敗北条件を追加する。
- 宝具未設定の敵のチャージを進行させる。
- 敵行動の未設定をエラーとして戦闘停止させる。
- 控え中の状態ターンやCTを進行させる。
- 端数処理を画面側の都合で変更する。
- 固定シードで再現できない乱数処理を追加する。
- 無関係なファイルを同じ変更へ混在させる。
