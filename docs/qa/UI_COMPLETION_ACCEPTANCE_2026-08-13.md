# UI完成仕様 統合受入報告

- 実施日: 2026-08-13
- 基準mainコミット: `b4725afb38120329f42862116097e7bb173e8f41`
- 対象: D-077～D-085、実画面指摘によるD-086～D-088追補
- 判定: 最終合格（D-088自動受入・ユーザー実画面受入完了）

## 受入結果

- 初期設定4タブ、最終再臨Lv・宝具Lv1初期値、ランダム／固定シード、開始前の確定文字列生成を確認した。ランダムへ切り替えた場合は以前の固定値を再利用しない。
- 戦闘画面6領域の順序、味方3タブ、PCの敵・味方3列／カード5列、スマートフォン横スクロール、56px操作領域を静的DOM・CSS検査で確認した。
- 味方のクラススキル／概念礼装／その他／合算、敵の通常／特殊／合算、初期タブ、アイコン中心一覧、その他の残りターン・回数バッジ、登録済み説明、適用済み値、解除可否、合算内訳を確認した。倍率permilleは表示だけ1/10の百分率、固定値は元単位を維持する。
- スキル短押し、長押し／詳細、理由付き無効化、単体・オーダーチェンジ対象モーダル、Esc・外押下・フォーカス循環をコードとDOM境界で確認した。
- カード最大3枚、3枚時の関連操作ロック、前衛枠・所有者、宝具上段／通常カード下段、宝具長押し／詳細、効果別説明行を確認した。Quick先頭は1枚選択時点で後続候補の保存済み基礎率へ20%を合算し、解除・先頭変更で戻す。Mightyも選択済み通常カードへ合算し、表示率は100%上限とする。
- 4区分ログ、ログ内確定シード、確定済みHP・NP・チャージ差分、チェイン→敵・味方HP・味方NP増減→ターン終了→Wave突破の順序、前へ／次へによる手動切替、確定前後HP・NPバーのアニメーション、確定ログの対象別総ダメージ、前後操作以外の入力遮断を確認した。HP差分をダメージとして表示せず、再生用表示状態はBattleSessionへ保存しない。
- 戦闘画面を残すリザルト、要約付き保存・再開、未確定入力／再生中の保存禁止、読込失敗時の非破壊表示を確認した。
- BattleSession、中断保存形式4、データ1.38.0、敵データ形式1、行動ログ形式5、ターンログ形式2、直接再開、固定シード検証リプレイ、カード再配布を維持した。具体コンテンツは追加していない。
- 資料に個別率がないスキル・宝具の弱体デメリットを基礎付与率500%とする規則を追加した。ルシフェラ「虚栄の女王」は対魔力17.5%を適用後の482.5%で状態欄へ成立し、`DelayedDebuff`で表示され、ターン終了時に共通強化解除を実行することを統合テストで確認した。弱体無効と強化解除耐性は既存共通処理を維持する。
- 初期設定の概念礼装欄下に「wikiを開く」、戦闘中の味方名に登録済み主参照リンクを追加した。光のコヤンスカヤとルシフェラのスキル・宝具説明を参照Wikiの効果順・接続表記へ合わせ、登録済み倍率を効果ごとの行に維持した。

## 自動検査

- `npm test`: 成功（51ファイル・525テスト、文書・指定画像・レスポンシブCSS検査を含む）
- `npm run typecheck`: 成功
- `npm run build`: 成功
- `git diff --check`: 成功

## 指定画像と未指定表示

- PRへ追加する画像は、アップロード済みassetsのうち明示指定されたスキル10点・状態26点だけとした。別画像の自動割当は行っていない。
- 追加指定に従い、Artsカード性能アップ=`Artsupstatus`、Quickカード性能アップ=`Quickupstatus`、スター発生率アップ=`Stargainup`、精神異常耐性アップ=`Resistanceup`、与ダメージプラス=`Powerup`へ接続した。
- 実画面で画像が読み込めない原因は、公開ベース`/FGO_Battle_Simulator_Work/`に対して画像URLだけが`/assets/...`を参照していたことだった。Viteの配信ベースURLを使う参照へ変更し、公開画面では`/FGO_Battle_Simulator_Work/assets/...`から読み込む。
- 現在の登録済み初期状態で文字表示となる未指定名称はない。将来の未指定名称は引き続き文字表示とする。

## 実画面受入

- クラウドブラウザーはローカルURLと一時単一HTMLをURL安全制限で開けなかったため、安全制限を迂回せず、開発側の確認は自動検査、コード検査、静的DOM・レスポンシブCSS検査までとした。
- ユーザー環境ではD-086～D-088の各追補を実画面で順次確認し、画像表示、配置、状態・合算・期限、カードとQuick始動表示、手動演出、遅延強化解除、Wiki導線、説明表記について報告された不満点を修正した。
- 2026-08-14、ユーザーから「現状の不満点はあらかた解消したため、ここで完成とする」と最終判断が示された。これを実画面受入の合格として記録し、UI完成仕様の追加確認点を完了とする。
- ユーザーから個別の全項目を再列挙したチェック結果は受領していないため、開発側が未実施の実機操作を確認済みとは扱わない。最終合格は、一連の実画面指摘の解消とユーザーによる完成判断を根拠とする。

## 次の作業

v1.0初期完成範囲外の具体コンテンツ追加対象を1件だけ選定する。
