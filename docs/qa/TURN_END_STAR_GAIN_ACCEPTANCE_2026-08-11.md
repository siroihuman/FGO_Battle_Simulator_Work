# ターン終了トリガーによるスター獲得 統合受入検査報告

検査日: 2026-08-11

基準mainコミット: `e5b13b7981aef49f9d300a967609fdcc4a5771f7`

判定: 合格

検出不具合: 0件

## 検査方法

- 自動検査: D-073の登録、候補固定、逐次加算、99個上限、ログ、保存・再開・リプレイに関係する対象テスト、全テスト、型検査、ビルド、文書整合検査を実行した。
- コード検査: `starState.ts`、`turnEnd.ts`、`turnEndCoordinator.ts`、`turnLog.ts`、`session.ts`、効果登録・トリガー実行、`battlePresentation.ts`をD-073と詳細仕様へ照合した。
- テスト用宣言データ: 具体的なサーヴァント・敵・概念礼装データは追加せず、共通処理の発動はテスト専用の`turn_end / gain_stars / next_command`宣言だけを使用した。
- 実ブラウザー検査: ローカルViteサーバーを`127.0.0.1`で起動したが、クラウドChromeからローカルURLへの接続が`ERR_BLOCKED_BY_CLIENT`で遮断されたため、画面操作は実施できなかった。UIは確定ログ値を改変した表示入力による自動検査、コード検査、および既存Chromium受入報告を根拠とした。

## 受入結果

| # | 検査項目 | 結果 | 受入根拠 |
|---:|---|---|---|
| 1 | 登録可能宣言 | 合格 | 味方・敵の`turn_end`、`self / single`、非負の安全な整数、`next_command`を登録できる。 |
| 2 | 不正宣言の事前拒否 | 合格 | `command`、不正対象、負数、小数、`turnEndSettlement`併用、必須項目欠落を`RangeError`で拒否し、効果採番を進めない。 |
| 3 | 未接続実行の拒否 | 合格 | 戦闘状態コーディネーターなしでスター要求を解決しようとした場合は、スター・乱数・採番を変更せず拒否する。 |
| 4 | 候補スナップショット | 合格 | 終了開始時の生存前衛だけを候補にし、控え、途中登場、途中追加状態を同じ終了処理で発動させない。 |
| 5 | 実行直前再確認 | 合格 | 所持者の死亡・前衛外、状態消失、条件不一致を再確認し、既存の型付き不発結果へ残す。 |
| 6 | 発動順 | 合格 | 前衛枠、同一個体の優先度、登録順、子アクション記述順を維持し、各寄与を合算せず即時処理する。 |
| 7 | 99個上限 | 合格 | 各加算で99個へ制限し、後続寄与は更新後の残量を参照する。99個到達後も要求量、前後、実加算量0、上限超過量を保存する。 |
| 8 | 保存先と繰上げ | 合格 | 味方・敵終了とも`nextCommandStars`だけへ加算し、既存チェックポイントで味方入力へ進む場合だけ現在使用分へ繰り上げる。戦闘終了時は繰り上げない。 |
| 9 | 終了処理順 | 合格 | ブレイク、HP同時精算、死亡・交代・退場、チャージ、期限、CT、補充、Wave・勝敗判定の既存順序を変更していない。 |
| 10 | 乱数・効果採番 | 合格 | スター加算と上限計算は6乱数列と効果採番を消費しない。親発動率だけ既存の`effects`列を使う。 |
| 11 | 確定ターンログ | 合格 | ターンログ形式2の既存子アクションへ区分、要求量、加算前、実加算量、加算後、上限超過量を任意保存し、行動ログ形式5へ別行動を追加しない。 |
| 12 | UI非再計算 | 合格 | `battlePresentation.ts`は保存済みスター加算値だけを整形する。改変した表示用ログ値を補正せず表示する自動検査に合格した。 |
| 13 | 保存・直接再開 | 合格 | 両スター区分、状態、採番、6乱数列、ターンログを形式4へ保存し、直接再開で終了時効果・繰上げ・ログ追加を二重実行しない。 |
| 14 | 固定シードリプレイ | 合格 | 開始状態、テスト宣言、操作、シードから最終状態、採番、6乱数列、ターンログが保存値と完全一致する。 |
| 15 | 形式番号 | 合格 | 中断保存形式4、データ1.38.0、敵データ形式1、行動ログ形式5、ターンログ形式2を維持した。 |
| 16 | 既存回帰 | 合格 | 固定スター獲得、攻撃スター、ターン終了HP精算、ブレイク、Wave、勝敗判定、保存・リプレイの全テストに合格した。 |
| 17 | 具体コンテンツ非追加 | 合格 | サーヴァント、敵、概念礼装の本番データは変更していない。 |
| 18 | v1.0初期完成範囲 | 合格 | 方針変更に従い共通処理を初期完成範囲へ追加し、この受入完了後の同範囲未実装を0件とした。 |

## 自動検査結果

- 対象テスト: 5ファイル、63テスト成功
- `npm test`: 50ファイル、514テスト成功
- `npm run typecheck`: 成功
- `npm run build`: 成功
- `git diff --check`: 成功
- 実ブラウザー: Vite起動成功。クラウドChromeからローカルURLへの接続は`ERR_BLOCKED_BY_CLIENT`の環境制約で未実施

## 不具合と受入上の制約

再現条件、期待値、実結果を記録すべき製品不具合は検出されなかった。保存形式・データ版・敵データ形式・ログ形式の変更は不要である。

具体コンテンツが未登録のため、共通処理の直接発動はテスト専用宣言データを使用した。実ブラウザーでは接続制約により戦闘開始、ターン終了、スター・ターンログ表示、中断保存、直接再開を再操作できなかった。これらは自動統合検査、UI非再計算検査、コード検査、および同じ形式4・データ1.38.0を実操作済みの既存Chromium受入報告で確認した。

## v1.0初期完成範囲と次作業

今回追加されたターン終了スター獲得共通処理を含め、v1.0初期完成範囲の未実装は0件である。正本の未実装には、初期2騎以外の具体的なサーヴァントデータと初期対象以外の具体的な敵データだけが残る。

次の1件は、v1.0初期完成範囲外の具体コンテンツ追加対象をサーヴァントまたは敵から1件だけ選定する作業とする。次作業では候補、既存共通処理への適合性、必要な一次出典を整理してユーザー判断を求め、対象決定前にデータ・実装・形式番号を変更しない。
